`tracewarden-demo-hashing.pt` - StreamGuard, all feature groups, trained on anonymized AgentDrift
(task-disjoint train split, CC BY 4.0) with the offline `hashing-768` encoder, conformally calibrated at
alpha = 0.01 on the validation split. Demo only: it has never seen real agents. Train the paper model with
all-mpnet-base-v2 embeddings (notebooks/kaggle/01).
